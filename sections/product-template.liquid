<!-- sections/product-template.liquid -->
<section class="product-detail-container">
  <div class="product-images">
    {% for media in product.media %}
      <img src="{{ media | image_url: width: 800 }}" alt="{{ media.alt | escape }}" />
    {% endfor %}
  </div>

  <div class="product-info">
    <p>{{ product.selected_or_first_available_variant.sku }}</p>
    <h1>{{ product.title }}</h1>
    <div class="price">¥{{ product.price | money_without_trailing_zeros }} <span style="font-size: 13px;">(tax in)</span></div>

    {% if product.options_with_values.size > 0 %}
    <div class="size-options">
      <p>Size: 
        {% for option in product.options_with_values %}
          {% if option.name == "Size" %}
            {% for value in option.values %}
              {{ value }}
            {% endfor %}
          {% endif %}
        {% endfor %}
      </p>
    </div>
    {% endif %}

    <form method="post" action="/cart/add">
      <input type="hidden" name="id" value="{{ product.variants.first.id }}" />
      <button type="submit" class="add-to-cart">ADD TO BAG</button>
    </form>

    <div class="detail-toggle">
      <h3 id="detailToggleBtn" style="cursor: pointer;">Detail ＋</h3>

      <div id="detail-content" style="display: none;">
        <table style="font-size: 14px; margin-bottom: 16px;">
          <tr><td>着丈</td><td>32cm</td></tr>
          <tr><td>身幅</td><td>33cm</td></tr>
          <tr><td>裾幅</td><td>29cm</td></tr>
        </table>
        <p><strong>素材：</strong>ポリエステル100%</p>
        <p><strong>原産国：</strong>MADE IN KOREA</p>
      </div>

      <div class="always-visible" style="margin-top: 20px;">
        <p>*お取り扱いの御注意<br>
        ・蛍光材の入っていない洗剤をご使用下さい。<br>
        ・タンブラー乾燥はお避け下さい。<br>
        ・洗濯の際に色落ちすることがありますので、他の商品との洗濯はお避け下さい。（単独洗い）<br>
        ・脱水後は直ちに取り出し、形を整えてから干してください。<br>
        ・長時間濡れたまま放置しないでください。<br>
        ・素材の特性上、汗や雨等で濡れた時や強く擦られた場合、摩擦により色落ちする場合がありますので御注意ください。<br>
        ・付属部分のアイロンはお避けください。</p>
        <p>モデル身長：160cm　着用サイズ：F</p>
      </div>
    </div>
  </div>
</section>
